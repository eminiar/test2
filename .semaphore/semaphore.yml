version: v1.0
name: Parallel
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Test
    task:
      secrets:
        - name: private-repo
      prologue:
        commands:
          - checkout
          - chmod 0600 ~/.ssh/id_rsa_semaphoreci
          - ssh-add ~/.ssh/id_rsa_semaphoreci
          - ls -la
          - 'git clone git@github.com:eminiar/infra.git'
      jobs:
        - name: Test
          commands:
            - ls -la
